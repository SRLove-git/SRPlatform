# 示例课程

目录下的每个 JSON 定义一门可加载课程：

- `rc_car.json`：RC 小车，脚本控制前进/停止/转向。
- `line_follower.json`：循迹小车，用左右距离传感器沿障碍物边缘行驶。
- `quadcopter.json`：四旋翼悬停，PD 高度保持。
- `robotic_arm.json`：机械臂，舵机控制关节完成动作循环。

字段说明见 `docs/course-format.md`。
